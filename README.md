### To install all dependencies run:
`npm install`

### To start run: 
`npm start`

### To test run:
`npm test`

### To build for production run:
`npm run build`

### To apply formatting: 
`npm run prittify`

### To apply check: 
`npm run lint`
